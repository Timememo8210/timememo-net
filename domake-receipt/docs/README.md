# Domic Home Passport source access

The cloud application's source code is no longer distributed as a ZIP from this public website. It will be shared with authorised collaborators through a private GitHub repository.

The private repository has not been created yet; its creation and initial push are awaiting the project owner's approval. Request access from the project owner once it is available.

Use local paths or private repositories for project handoffs. Do not use this public website to transfer source archives, credentials, or user documents.

Removing the current ZIP does not remove earlier copies from Git history. No history rewrite is included in this change.

## 中文说明

云端应用源码不再通过本公开网站的 ZIP 分发，将通过私有 GitHub 仓库向获授权的合作方共享。

私有仓库尚未创建，创建及首次推送正在等待项目所有者确认；建立后请向项目所有者申请访问。

项目交接使用本地路径或私有仓库，不使用本公开网站中转源码包、凭据或用户文档。

本次只移除当前 ZIP，历史副本仍留在 Git 历史中，不改写历史。
