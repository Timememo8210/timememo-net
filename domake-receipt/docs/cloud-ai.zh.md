# 云端 AI 接入记录 · 2026-09-12

当前建议：先通过 OpenRouter 使用 **Gemini 3.1 Flash Lite**。它适合低成本、快速的图片/PDF信息提取。2.5 Flash Lite 更便宜，作为同一套真实票据的对照；3.5 Flash Lite 作为升级候选；3.8 Flash 留作复杂案例对照。本轮已进行了小批量真实调用，仍建议 3.1 Flash Lite 作为试用默认，详见[模型实测与网页流程](../ai-pilot/zh.html)。

## 成本比较

美元，每百万 token，标准服务价格；查阅日期 2026-09-12。

| 模型 | 输入 | 输出 | 每一万张示意成本 | 建议用途 |
|---|---:|---:|---:|---|
| [2.5 Flash Lite](https://openrouter.ai/google/gemini-2.5-flash-lite) | $0.10 | $0.40 | $3.50 | 最低价格对照，验证清晰票据是否够用 |
| [3.1 Flash Lite](https://openrouter.ai/google/gemini-3.1-flash-lite) | $0.25 | $1.50 | $11.25 | 第一版默认候选 |
| [3.5 Flash Lite](https://openrouter.ai/google/gemini-3.5-flash-lite) | $0.30 | $2.50 | $17.00 | 比较是否能减少漏项和人工修改 |
| [3.8 Flash](https://openrouter.ai/google/gemini-3.8-flash) | $0.75 | $3.75 | $30.00 | 复杂版式和模糊票据的对照候选 |

示意按每张 1,500 输入、500 计费输出 token 计算。实际文件页数、图片处理、提示词和逐字段证据会改变用量；推理 token 也可能计费。表中没有包含重试、充值手续费、存储和其他托管费用，不是报价或本项目的测量值。3.8 当前价格为截至 2026-12-31 的优惠价，之后需重新核对。

## 本地 OCR 与云端模型的差别

现有网页使用浏览器里的 Tesseract OCR 加规则提取：它先读出文字，再按日期、金额等模式抓字段，不需要用户 Mac 运行一个本地大模型服务器。困难主要在语义：区分客户和实际工人、办公室与施工地址、报价与实际付款、账单日期与服务日期。多模态大模型直接阅读文档并整理这些关系，但依然可能误读或补全，因此仍要保留原件、缺失字段和人工确认。

云端 API 的方案不依赖网站作者的 Mac 开机；用户浏览器上传到云端接口，由接口调用模型。API Key 不放在网页里。

## 已实现的代码

给 Daniel 的[云端接口源码包](cloud-ai-service-source.zip)包含本轮实际使用的接口、提示词、规范和测试；不含密钥、试用码或托管账户配置。前端源码、字段规范与报告在同一 GitHub 项目中。

- 英文默认、中文切换；识别方式可选云端 AI、本地 OCR、手工录入。
- 独立试用访问码、文件类型/大小检查、超时和可恢复错误。
- 固定输出 JSON、缺失值 null、逐字段证据、公司与实际服务人分开。
- 上传识别 → 用户查看/修改 → 勾选确认 → 保存原件与记录。没有确认不进入已确认记录。
- 保存后的原件可以重新识别，仍需再次确认；不会由模型直接决定用户或房屋归属。

## 当前明确限制

用户提供的 API Key 已验证有效并配置为服务端密钥。**真实云端接口已经接通，默认 Gemini 3.1 Flash Lite**；图片与 PDF 均已成功调用。与 2.5 Lite、3.5 Lite、3.8 Flash 的小样本结果及费用已保存到本轮报告。既有旧截图报告继续对应此前本地 OCR 和人工确认流程。

另外，当前保存仍然是此浏览器的 IndexedDB；管理页仍是本机记录视图。接入 AI 不等于已经实现所有用户共享的云数据库。开放给大量客户前需要账户权限、持久化配额，以及以房屋为主体的云端记录和附件存储。

## 下一步验收

小批量已覆盖清晰 PDF、模拟手机照片、信息缺失、报价、订金与余额、多个人名、严重模糊及无关照片。后续需要更大且来自真实业务的样本集、逐用户额度和云数据库。测试结束时已核对当前 Key 的独立额度上限为 10 美元；该额度与账户充值余额分开计算。试用访问码不要公开发布，面向更多客户前应补充持久化配额。模型实测是单次小样本观察，不保证所有文件或后续调用表现相同。
